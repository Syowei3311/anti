<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd


                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context-4.0.xsd
                        http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd"
       default-lazy-init="false">
    <import resource="spring-mvc.xml"/>
    <!-- 配置自动扫包 -->
    <context:component-scan base-package="dna.controller,dna.service,dna.dao"/>

    <context:property-placeholder location="classpath:config.properties"/>

    <!-- 提供方应用名称信息，这个相当于起一个名字，我们dubbo管理页面比较清晰是哪个应用暴露出来的 -->
    <dubbo:application name="anti_base"></dubbo:application>
    <!--<dubbo:provider delay="-1" timeout="6000" retries="0"/>-->
    <!-- 用dubbo协议在20880端口暴露服务 -->
    <!--<dubbo:protocol name="rest" port="20880" server="servlet"/>-->
    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry address="${registry.address}" protocol="zookeeper" check="false" subscribe="false"
                    register="true"></dubbo:registry>
    <!-- 要暴露的服务接口 -->
    <dubbo:service interface="dna.remote.HelloService" ref="helloService"/>

    <!--&lt;!&ndash;<dubbo:application name="dubbo_consumer"></dubbo:application>-->
    <!--&lt;!&ndash; 要引用的服务 &ndash;&gt;-->
    <!--<dubbo:reference interface="dna.controller.HelloServiceImpl" id="HelloService"></dubbo:reference>&ndash;&gt;-->
    <bean id="appContextHolder" class="dna.persistence.commons.AppContextHolder"></bean>
</beans>